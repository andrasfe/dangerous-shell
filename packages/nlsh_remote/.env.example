# nlsh-remote Configuration

# Ed25519 Mode (recommended)
# Path to MCP server's public key for signature verification
# Copy from MCP server machine after key generation
NLSH_MCP_PUBLIC_KEY_PATH=~/.nlsh/keys/mcp_public.key

# Legacy HMAC Mode (if not using Ed25519)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# NLSH_SHARED_SECRET=your_shared_secret_here

# Server binding (127.0.0.1 = localhost only, use SSH tunnel for security)
NLSH_REMOTE_HOST=127.0.0.1

# Server port
NLSH_REMOTE_PORT=8765

# Shell to use for command execution
# NLSH_SHELL=/bin/bash
